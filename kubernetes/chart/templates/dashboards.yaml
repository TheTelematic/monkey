apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name}}-grafana-dashboards
data:
  {{- range $key, $value := .Values.loadDashboards }}
  {{ $key }}: |-
    {{ $value | nindent 4 }}
  {{- end }}
