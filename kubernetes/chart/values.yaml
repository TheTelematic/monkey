image:
  name: monkey
  tag: latest
  policy: IfNotPresent


replicas:
  api:
    min: 1
  consumers:
    translations:
      min: 1

service:
  port:
    container: 8000
    name: http
    port: 80

ollama:
  enabled: false
  image:
    name: ollama/ollama
    tag: 0.1.36
    policy: IfNotPresent
  port: 11434

config:
  LOG_LEVEL: "INFO"
  OLLAMA_URL: ""
  OPENAI_API_KEY: ""
  LLM_ENGINE: ""
  REDIS_HOST: "redis-master"
  RABBITMQ_URL: "amqp://monkey:monkey@rabbitmq:5672/"

ingress:
  enabled: true
  host: "monkey.local"
